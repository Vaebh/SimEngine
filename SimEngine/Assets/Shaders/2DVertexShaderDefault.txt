#version 150
in vec2 texcoord;
in vec4 position;

out vec2 Texcoord;

uniform mat4 model;
uniform vec2 frameInfo;

void main()
{
	Texcoord = vec2(frameInfo.x + (texcoord.x * frameInfo.y), texcoord.y);
	gl_Position = model * position;
}